# dashboard-frontend

Chưa dựng — sẽ scaffold Next.js ở Phase 6 (ROADMAP.md), khi có model đã train và API serving
predictions sẵn sàng. Đây là app cho sản phẩm chính (hotelier view + consumer view), tách biệt
với `scraper-frontend` (tool nội bộ). Cùng convention với `scraper-frontend`, cùng gọi chung
1 backend (`../backend`).
